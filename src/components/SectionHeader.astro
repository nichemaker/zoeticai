---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  href?: string;
  linkLabel?: string;
}

const {
  eyebrow,
  title,
  description,
  href,
  linkLabel = "View all",
} = Astro.props;
---

<div
  class="mb-9 flex flex-col gap-5 sm:mb-11 sm:flex-row sm:items-end sm:justify-between sm:gap-10"
>
  <div class="max-w-2xl">
    {
      eyebrow && (
        <p class="eyebrow">{eyebrow}</p>
      )
    }
    <h2
      class:list={[
        "text-section-title",
        eyebrow ? "mt-2.5" : "",
      ]}
    >
      {title}
    </h2>
    {
      description && (
        <p class="mt-3 max-w-xl text-[0.9375rem] leading-relaxed text-ink-muted sm:text-base">
          {description}
        </p>
      )
    }
  </div>
  {
    href && (
      <a
        href={href}
        class="group inline-flex shrink-0 items-center gap-1.5 self-start text-sm font-semibold text-accent transition-colors hover:text-accent-hover sm:self-auto"
      >
        {linkLabel}
        <span
          aria-hidden="true"
          class="transition-transform duration-200 ease-[var(--ease-out-soft)] group-hover:translate-x-0.5"
        >
          →
        </span>
      </a>
    )
  }
</div>
